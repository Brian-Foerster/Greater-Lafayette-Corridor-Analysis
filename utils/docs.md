Docs for `utils/`

Purpose
- Utility helpers: logutil, misc, networks, sampling, testing, yamlio.

What to add
- Document utility functions that are used across the project and expected inputs/outputs.

Tests
- Tests are under `utils/tests/` and include network and sampling tests. Add fixtures when needed and keep tests deterministic.